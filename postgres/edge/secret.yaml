apiVersion: v1
kind: Secret
metadata:
  name: edge-db-secret
type: Opaque
stringData:
  # In production, use ExternalSecrets or sealed-secrets
  POSTGRES_USER: "app-user"
  POSTGRES_PASSWORD: "edge-password-change-me"
  POSTGRES_DB: "app_db"
