apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: cnpg
  namespace: flux-system
spec:
  releaseName: cnpg
  chart:
    spec:
      chart: cloudnative-pg
      sourceRef:
        kind: HelmRepository
        name: cnpg-oci
        namespace: flux-system
  interval: 1h
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
