apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-pod-config
data:
  # Custom PostgreSQL configuration
  # These values are injected into postgresql.conf
  max_connections: "100"
  shared_buffers: "32MB"
  work_mem: "4MB"
