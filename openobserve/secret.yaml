apiVersion: v1
kind: Secret
metadata:
  name: openobserve-auth
  namespace: openobserve
type: Opaque
stringData:
  # Keys must match the emailKey and passwordKey in the HelmRelease
  ZO_ROOT_USER_EMAIL: "admin@example.com"
  ZO_ROOT_USER_PASSWORD: "password123"
