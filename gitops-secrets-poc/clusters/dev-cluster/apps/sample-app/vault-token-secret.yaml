apiVersion: v1
kind: Secret
metadata:
  name: vault-dev-root-token
  namespace: sample-app
  annotations:
    note: "PoC only: encrypt with SOPS in Git; replace with Vault Kubernetes auth in production"
type: Opaque
stringData:
  token: root
