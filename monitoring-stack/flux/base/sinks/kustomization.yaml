apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - secrets.yaml
  - source.yaml
  - release.yaml

labels:
  - pairs:
      orise.io/tenant: common
      orise.io/environment: dev
